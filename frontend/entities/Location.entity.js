class Location {
    constructor({
      id,
      latitude,
      longitude,
      fullAddress,
    }) {
      this.id = id;
      this.latitude = latitude;
      this.longitude = longitude;
      this.fullAddress = fullAddress;
    }
  }
  
  export default Location;